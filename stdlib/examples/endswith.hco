function endsWith(source, text)
{
    if (right(source, count(text)) == text)
    {
        return 1;
    }
    return 0;
}

printLn(endsWith("Hello World!", "World!")); /* Expected true */
printLn(endsWith("Lorem ipsum", "World!")); /* Expected false */
function or(left, right)
{
    if (left || right)
    {
        return 1;
    }
    return 0;
}

printLn(or(1,1)); /* Expected true */
printLn(or(1,0)); /* Expected true */
printLn(or(0,1)); /* Expected true */
printLn(or(0,0)); /* Expected false */
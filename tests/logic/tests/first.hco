function first(value)
{
  if (count(value) > 0)
  {
      return value[0];
  }
  return "";
}

printLn(first("hello")); /* Expected h */
let items = array();
items[0] = "First item";
items[1] = "Second item";
printLn(first(items)); /* First item */